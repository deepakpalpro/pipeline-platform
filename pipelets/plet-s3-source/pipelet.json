{
  "id": "plet-s3-source",
  "name": "S3 Source",
  "version": "1.0.0",
  "runtime": "Python",
  "imageRef": "pipeline-platform/plet-s3-source:local",
  "category": "Source",
  "description": "Ingest a single object from an S3-compatible bucket. Required Keys are filled at pipeline step level; bucket/credentials come from bound connector and service.",
  "requiredDeploymentKeys": ["region"],
  "requiredExecutionKeys": ["objectKey"],
  "deploymentConfiguration": {
    "cloud": "aws",
    "region": "",
    "endpoint": ""
  },
  "executionConfiguration": {
    "objectKey": "",
    "batchSize": "1"
  },
  "connectorType": "storage",
  "env": {
    "CONNECTOR_CONFIG": "Storage connector JSON (required: bucket; optional: endpoint, accessKeyId, secretAccessKey)",
    "SERVICE_CONFIG": "Auth service JSON (optional: accessKeyId, secretAccessKey)",
    "DEPLOYMENT_CONFIG": "Step/pipeline deployment overrides (required after merge: region)",
    "EXECUTION_CONFIG": "Step/pipeline execution overrides (required after merge: objectKey)",
    "PIPELET_DEFAULTS": "Optional JSON with deploymentConfiguration / executionConfiguration defaults"
  }
}
