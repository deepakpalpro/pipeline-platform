apiVersion: 1

providers:
  - name: pipeline-platform
    orgId: 1
    folder: Pipeline Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
